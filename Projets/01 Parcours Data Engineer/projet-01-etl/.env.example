# MODÈLE de configuration — ce fichier-ci PEUT être commité (il ne contient aucun secret).
#
# À faire : copie-le en ".env" (dans le même dossier) et mets tes vraies valeurs dedans.
#   Windows : copy .env.example .env
# Le fichier ".env" est ignoré par git (voir .gitignore) : tes identifiants ne partiront jamais sur GitHub.

DB_USER=postgres
DB_PASSWORD=Vivi2001?!
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
